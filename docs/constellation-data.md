# 星座テンプレートデータ仕様書

## 1. 目的

なぞり判定の比較対象となる「お手本の形」を定義する。

- **実在の星座(`real`)**: 主要星の実際の位置(J2000 の赤経・赤緯)を平面投影して
  0〜100 座標に正規化したもの。実際の夜空・星図で見える形とほぼ同じプロポーション
  で、なぞる順序も一般的な星のつなぎ方(例: 北斗七星はドゥーベ→…→アルカイド)に
  合わせている。頂点は主要星のみに絞り、1ストロークでなぞれるようにしている。
- **おはなしの星座(`fun`)**: アプリオリジナルの形。子供が喜ぶモチーフを
  一筆書きできるシルエットとしてデザインしたもの。

## 2. データ構造

`src/data/constellations.ts` に以下の型で星座を定義する(最新の収録数・一覧は同ファイルを参照)。

```ts
export type ConstellationCategory = 'real' | 'fun';

export interface Constellation {
  id: string;                   // 一意なID (例: "ursa-major")
  nameJa: string;               // 表示名 (例: "おおぐま座")
  nameEn: string;               // 通称英語名 (例: "Big Dipper (Ursa Major)")
  latinName: string;            // 学名 (例: "Ursa Major")
  category: ConstellationCategory; // 'real'=実在 / 'fun'=おはなしの星座(非実在)
  emoji: string;                // 図鑑カード等に添える絵文字
  hint: string;                 // 未発見の図鑑カードに出す「なぞる形」のヒント
  description: string;          // 結果画面・図鑑に表示する子供向けの紹介文
  path: Point[];                // なぞる順に星を直線で結んだ座標列(1ストローク分)
}
```

- `path` は「星をこの順番で直線に結ぶとその星座の形になる」という頂点列。
- 座標系は任意単位の 0〜100 四方(相対値)。マッチング時に平行移動・拡大縮小・回転を
  正規化して比較するため、絶対座標やスケールに意味はない。
- 同じ点に戻る(例: 十字形やひし形を1ストロークで描く)場合は `path` 内に同じ座標を
  複数回含めてよい。
- `category` は結果画面・図鑑での色分けと絞り込みに使う。`fun` はこのアプリオリジナルの
  非実在の星座(ふざけた星座)で、学名(`latinName`)も遊びで付けたもの。

## 3. 収録する星座と形状の設計方針

### 3.1 実在の星座(`category: 'real'`)

新規追加時はこの表の末尾に1行追記する(連番は振らない・並び順に意味はない)。

| id | 表示名 | 形の特徴 |
|---|---|---|
| `ursa-major` | おおぐま座(北斗七星) | ひしゃく(柄杓)型のジグザグ |
| `ursa-minor` | こぐま座 | 小さいひしゃく型 |
| `cassiopeia` | カシオペヤ座 | アルファベットの「W」 |
| `orion` | オリオン座 | 砂時計(ちょうちょ結び)型 |
| `scorpius` | さそり座 | 先端が巻き上がる「J」字カーブ |
| `cygnus` | はくちょう座(北十字) | 十字(中心を3回通過) |
| `lyra` | こと座 | 小さなひし形(たこ型) |
| `aquila` | わし座 | 横長のひし形(たこ型) |
| `gemini` | ふたご座 | 頭が寄り添い足が開く2本足型 |
| `taurus` | おうし座 | 角が伸びた浅い「V」字 |
| `leo` | しし座 | 鎌(?マーク)+三角のしっぽ |
| `pegasus` | ペガスス座(秋の四辺形) | 正方形 |
| `cepheus` | ケフェウス座 | とんがり屋根の家(五角形) |
| `delphinus` | いるか座 | ひし形+しっぽ |
| `aquarius` | みずがめ座 | 肩から水がめ(小さな輪)を経て水の流れが伸びる形 |
| `virgo` | おとめ座 | 中心から複数方向に枝分かれするジグザグ(スピカが末端) |
| `aries` | おひつじ座 | ゆるく曲がる長いかぎ型(4つ星) |
| `cancer` | かに座 | 中心から枝分かれする「Y」字型 |
| `capricornus` | やぎ座 | はしが細く反対側が大きく広がる三角形 |
| `sagittarius` | いて座 | きゅうす(ティーポット)型の七角形 |
| `libra` | てんびん座 | 左右に開いた横長のはかり型 |
| `pisces` | うお座 | 小さな輪が2つ、長いリボンでつながる形 |

### 3.2 おはなしの星座(`category: 'fun'`・非実在)

新規追加時はこの表の末尾に1行追記する(連番は振らない・並び順に意味はない)。

| id | 表示名 | 形の特徴 |
|---|---|---|
| `mona-lisa` | モナリザ座 | 四角い うずまき |
| `heart` | ハート座 | ハート形(上のくぼみ+下のとがり) |
| `cat` | ネコ座 | 2つの三角の耳+顔の輪郭 |
| `rocket` | ロケット座 | 先のとがった機体+左右のフィン |
| `ramen` | ラーメン座 | ゆげのギザギザ+どんぶり |
| `lightning` | カミナリ座 | 稲妻のジグザグ(Z) |
| `ghost` | おばけ座 | 丸い頭+下がギザギザ |
| `mushroom` | キノコ座 | ドーム型のかさ+四角い柄 |
| `star` | ほし座 | 1本の線で結ぶ五芒星(交差する星形) |
| `moon-rabbit` | つきのうさぎ座 | 2つの耳のとがり+三日月のふくらみ |

実際の星座は星の並びを参考にしつつ、**一筆書きでなぞれる代表的なシルエット**に単純化している。
「おはなしの星座」は完全にオリジナルで、子供が喜ぶモチーフを一筆書きできる形にした。
いずれも天文学的な正確さより「子供が指でなぞって再現しやすく、他の星座と形が被らないこと」を優先している。
星座数の合計・カテゴリ別内訳は追記のたびに変わるため、この文書には書かない。最新の一覧・件数は
`src/data/constellations.ts` を参照する。

## 4. データの妥当性チェック(単体テスト)

`src/lib/shapeMatcher.test.ts` で全 `CONSTELLATIONS` をループして検証する。

- 各星座を「平行移動 / 拡大縮小 / 回転 / 向き反転」した入力が、いずれも自分自身に最も
  高いマッチ度で一致すること。
- **識別性**: どの星座も、全テンプレートの中で自分自身との距離が最小になること
  (=どの2つの形も十分に離れていること)。星座を追加するときは、このテストが通ることで
  既存の形と衝突しないことを保証する。

## 5. 将来の拡張

- 季節ごとの星座追加、88星座への拡張は `path` データを追加するだけで対応可能。
  追加後に §4 の識別性テストが通ることを必ず確認する。
- 星座ごとの物語・神話文は `description` フィールドを長文化するか、専用フィールドを足して対応する。
